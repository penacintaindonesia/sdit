import Image from "next/image";
import Link from "next/link";
import { ArrowLeft } from "lucide-react";
import { school } from "@/config/school";
import { aboutPage } from "@/data/about-page";
import Footer from "@/components/Footer";

export const metadata = {
  title: `Tentang Kami | ${school.name}`,
  description: aboutPage.paragraphs[0],
};

export default function TentangKamiPage() {
  return (
    <>
      <header className="border-b border-primary/10 bg-white">
        <div className="container-x flex h-[72px] items-center">
          <Link
            href="/"
            className="inline-flex items-center gap-2 text-sm font-semibold text-primary hover:text-secondary"
          >
            <ArrowLeft className="h-4 w-4" aria-hidden="true" />
            Kembali ke Beranda
          </Link>
        </div>
      </header>

      <main>
        <section className="bg-light py-14 sm:py-20">
          <div className="container-x">
            <p className="font-display text-lg italic text-green-dark">{aboutPage.intro}</p>
            <h1 className="mt-2 font-display text-[32px] font-extrabold text-primary sm:text-[42px]">
              {aboutPage.heading}
            </h1>
          </div>
        </section>

        <section className="py-14 sm:py-20">
          <div className="container-x grid gap-10 lg:grid-cols-[1fr,1.1fr] lg:items-start">
            <div className="overflow-hidden rounded-[26px] shadow-card">
              <Image
                src={aboutPage.image}
                alt={`Profil ${school.name}`}
                width={900}
                height={700}
                sizes="(max-width: 1024px) 100vw, 480px"
                className="h-full w-full object-cover"
              />
            </div>
            <div className="space-y-5">
              {aboutPage.paragraphs.map((p, i) => (
                <p key={i} className="text-[15px] leading-relaxed text-ink/75">
                  {p}
                </p>
              ))}
            </div>
          </div>
        </section>
      </main>

      <Footer />
    </>
  );
}